---
phase: 05-deployment-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [next.config.mjs, package.json]
autonomous: false
requirements: [DEP-01, DEP-02, DEP-03, DEP-04]

must_haves:
  truths:
    - "next build completes with zero TypeScript errors and zero build failures"
    - "Vercel project is linked to repo with auto-deploy on push to main"
    - "All 6 environment variables are set in Vercel production"
    - "Production URL serves homepage, model profile, and admin login without errors"
  artifacts:
    - path: "next.config.mjs"
      provides: "Valid Next.js config for production build"
    - path: ".vercel/project.json"
      provides: "Vercel project link"
  key_links:
    - from: "Vercel project"
      to: "GitHub repo"
      via: "vercel link + auto-deploy"
      pattern: "push to main triggers deploy"
    - from: "Vercel env vars"
      to: "Supabase + Resend"
      via: "runtime environment"
      pattern: "NEXT_PUBLIC_SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, etc."
---

<objective>
Fix any build issues so `npm run build` passes cleanly, then deploy to Vercel production with all environment variables set and all public routes accessible.

Purpose: Ship the complete application to production so photographers and casting directors can browse the model directory at a live URL.
Output: Clean production build, live Vercel deployment with all routes working.
</objective>

<execution_context>
@C:/Users/frenc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/frenc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@next.config.mjs
@package.json
@tsconfig.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix build issues and verify clean production build</name>
  <files>next.config.mjs, package.json, tsconfig.json</files>
  <action>
Run `npm run build` and fix any TypeScript errors, missing imports, or configuration issues until the build completes successfully.

Common issues to check and fix:
- TypeScript strict mode errors (missing types, implicit any)
- Missing or incorrect import paths
- next.config.mjs remotePatterns for Supabase Storage and Unsplash hostnames
- Any ESLint errors that block build
- Unused variables or imports that TypeScript strict mode flags

If build already passes cleanly, confirm and move on. Do NOT change application behavior — only fix build-blocking issues.
  </action>
  <verify>
    <automated>cd /c/Users/frenc/Projects/kpeachgirl-app && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>`npm run build` exits with code 0, producing a .next/standalone or .next build output with zero errors.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Vercel project setup, environment variables, and production deploy</name>
  <files>.vercel/project.json</files>
  <action>
Human must complete Vercel setup — Claude cannot authenticate to Vercel on the user's behalf.

1. Install Vercel CLI (if not already): npm i -g vercel
2. Link the project: Run `vercel link` in the project root. Select your Vercel account, create or link a project.
3. Set environment variables in Vercel Dashboard (Settings > Environment Variables) or via CLI:
   - vercel env add NEXT_PUBLIC_SUPABASE_URL production
   - vercel env add NEXT_PUBLIC_SUPABASE_ANON_KEY production
   - vercel env add SUPABASE_SERVICE_ROLE_KEY production
   - vercel env add RESEND_API_KEY production
   - vercel env add ADMIN_EMAIL production
   - vercel env add REVALIDATION_SECRET production
   Use the same values from your local .env.local file.
4. Deploy to production: vercel --prod
5. Smoke test the production URL:
   - Visit / — homepage loads with age gate, then model grid
   - Visit /model/{any-seed-slug} — model profile page loads
   - Visit /admin/login — admin login page loads
   - Visit /membership — membership form loads
6. Verify auto-deploy: Push a trivial commit to main and confirm Vercel triggers a new deployment.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" https://YOUR_PRODUCTION_URL/</automated>
  </verify>
  <done>Production URL serves all 4 routes (/, /model/[slug], /admin/login, /membership) with 200 status. Vercel Dashboard shows all 6 env vars set. Auto-deploy on push to main confirmed.</done>
</task>

</tasks>

<verification>
- `npm run build` completes with exit code 0
- Production URL returns 200 for `/`, `/model/[slug]`, `/admin/login`, `/membership`
- Vercel Dashboard shows all 6 environment variables set for production
- A push to main triggers automatic redeployment
</verification>

<success_criteria>
1. Clean TypeScript build with zero errors
2. Vercel project linked to repository with auto-deploy on main
3. All 6 env vars configured in Vercel production environment
4. Production URL serves all public routes and admin login without errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-deployment-launch/05-01-SUMMARY.md`
</output>
