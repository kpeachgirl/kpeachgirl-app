---
phase: 04-admin-cms
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/admin/login/page.tsx
  - app/admin/layout.tsx
  - lib/supabase/middleware.ts
  - middleware.ts
autonomous: true
requirements:
  - ADM-01
  - ADM-02
  - ADM-03
  - ADM-20

must_haves:
  truths:
    - "Navigating to /admin without auth redirects to /admin/login"
    - "Admin can log in with email/password via Supabase Auth"
    - "After login, admin is redirected to /admin dashboard"
    - "Admin can log out and is redirected to /admin/login"
    - "Non-admin authenticated users are also redirected away from /admin"
  artifacts:
    - path: "app/admin/login/page.tsx"
      provides: "Login page with Supabase Auth signInWithPassword"
      min_lines: 50
    - path: "app/admin/layout.tsx"
      provides: "Server-side auth guard with getUser() + is_admin check"
      min_lines: 20
    - path: "lib/supabase/middleware.ts"
      provides: "Extended updateSession with /admin route protection"
      min_lines: 30
  key_links:
    - from: "app/admin/login/page.tsx"
      to: "lib/supabase/client.ts"
      via: "createClient() for signInWithPassword"
      pattern: "signInWithPassword"
    - from: "app/admin/layout.tsx"
      to: "lib/supabase/server.ts"
      via: "createClient() server for getUser()"
      pattern: "getUser"
    - from: "lib/supabase/middleware.ts"
      to: "middleware.ts"
      via: "updateSession import"
      pattern: "updateSession"
---

<objective>
Build the admin authentication layer: login page, middleware protection, server layout guard, and logout.

Purpose: Gate all admin routes behind Supabase Auth so only the authenticated admin user can access the CMS.
Output: Working login flow at /admin/login, protected /admin layout, logout functionality.
</objective>

<execution_context>
@C:/Users/frenc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/frenc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-admin-cms/04-RESEARCH.md

Key existing files:
- lib/supabase/client.ts — browser Supabase client (createClient)
- lib/supabase/server.ts — server Supabase client (createClient)
- lib/supabase/middleware.ts — updateSession function for cookie refresh
- middleware.ts — root middleware calling updateSession
- lib/types.ts — all TypeScript interfaces
- lib/i18n.ts — translation keys (includes admin translations)

<interfaces>
From lib/supabase/client.ts:
```typescript
export function createClient(): SupabaseClient
```

From lib/supabase/server.ts:
```typescript
export function createClient(): SupabaseClient
```

From lib/supabase/middleware.ts:
```typescript
export async function updateSession(request: NextRequest): Promise<NextResponse>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin login page and server layout auth guard</name>
  <files>app/admin/login/page.tsx, app/admin/layout.tsx, app/admin/page.tsx</files>
  <action>
Create app/admin/login/page.tsx as a 'use client' component matching the prototype design (ModelDirectory.jsx lines 639-701):
- Kpeachgirl logo with rose accent, "Admin Portal" subtitle
- Email + password inputs with dark luxury styling (bg #181716, border var(--sand), font Manrope)
- Login button that calls supabase.auth.signInWithPassword({ email, password })
- Error display for invalid credentials (rose-tinted background)
- Loading state during authentication ("Authenticating...")
- On success: router.push('/admin') + router.refresh()
- "Back to Site" button linking to / (use next/link, not onClick navigation)
- Bottom text: "Authorized personnel only. All sessions are logged."
- Entrance animation: translateY(20px) to 0 with opacity transition on mount
- Input styling: placeholder text, onKeyDown Enter triggers login

Create app/admin/layout.tsx as a SERVER component:
- Import createClient from '@/lib/supabase/server'
- Call await supabase.auth.getUser() (NOT getSession — security requirement per research)
- If error or no user, redirect('/admin/login')
- Check user.app_metadata?.is_admin === true; if not admin, redirect('/admin/login')
- Pass children through: return <>{children}</>
- Add metadata export: { title: 'Admin | Kpeachgirl' }

Create app/admin/page.tsx as a minimal client component that renders placeholder text "Admin Dashboard" — this will be replaced in plan 04-02 with the full dashboard shell.
  </action>
  <verify>
    <automated>cd C:/Users/frenc/Projects/kpeachgirl-app && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
- /admin/login renders the styled login form
- app/admin/layout.tsx uses getUser() (not getSession) with is_admin check
- Unauthenticated access to /admin redirects to /admin/login
- Successful login redirects to /admin
  </done>
</task>

<task type="auto">
  <name>Task 2: Middleware admin route protection and logout</name>
  <files>lib/supabase/middleware.ts, middleware.ts</files>
  <action>
Extend lib/supabase/middleware.ts updateSession function (do NOT create a second Supabase client — pitfall #3 from research):
- After the existing `await supabase.auth.getUser()` call, capture the user result
- Add admin route check: if path starts with '/admin' AND is NOT '/admin/login':
  - If no user, return NextResponse.redirect(new URL('/admin/login', request.url))
  - The is_admin claim check is handled by the layout (belt + suspenders), middleware just checks authentication
- Return supabaseResponse as before for all other routes

The root middleware.ts stays the same (just calls updateSession), no changes needed there. But verify the matcher pattern includes /admin routes (it should — the existing pattern matches all non-static routes).

Add logout functionality: In app/admin/login/page.tsx (already created in Task 1), ensure there's no separate logout page needed — logout will be a button in the admin nav (plan 04-02) that calls supabase.auth.signOut() then router.push('/admin/login') + router.refresh(). For now, just ensure the auth infrastructure supports it.
  </action>
  <verify>
    <automated>cd C:/Users/frenc/Projects/kpeachgirl-app && npm run build 2>&1 | tail -20 && grep -c "getSession" app/admin/ -r 2>/dev/null; echo "getSession count (should be 0): $?"</automated>
  </verify>
  <done>
- Middleware redirects unauthenticated /admin/* requests to /admin/login
- /admin/login is NOT protected (excluded from redirect)
- No usage of getSession() anywhere in admin code (security requirement)
- Build passes with zero TypeScript errors
  </done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- `grep -r "getSession" app/admin/` returns no results (only getUser used)
- app/admin/login/page.tsx exists with signInWithPassword
- app/admin/layout.tsx exists with getUser + is_admin check
- lib/supabase/middleware.ts has admin route protection
</verification>

<success_criteria>
- Admin login page renders at /admin/login with prototype-matching dark luxury design
- Unauthenticated /admin access redirects to /admin/login via both middleware and layout guard
- Login calls Supabase Auth signInWithPassword and redirects to /admin on success
- getUser() used everywhere (never getSession) for JWT validation security
</success_criteria>

<output>
After completion, create `.planning/phases/04-admin-cms/04-01-SUMMARY.md`
</output>
