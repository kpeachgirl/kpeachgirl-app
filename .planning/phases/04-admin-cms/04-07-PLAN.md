---
phase: 04-admin-cms
plan: 07
type: execute
wave: 4
depends_on: ["04-03", "04-04"]
files_modified:
  - components/admin/SubmissionsTab.tsx
  - components/admin/AdminDashboard.tsx
autonomous: true
requirements:
  - ADM-11
  - ADM-12

must_haves:
  truths:
    - "Admin sees all submissions with name, email, status, and submitted date"
    - "Admin can advance submission status: new -> reviewed -> approved -> dismissed"
    - "Admin can convert an approved submission into a live model profile"
    - "Status badges use colored labels (new=rose, reviewed=peach, approved=sage, dismissed=sand)"
    - "Share link box shows membership form URL with copy-to-clipboard"
  artifacts:
    - path: "components/admin/SubmissionsTab.tsx"
      provides: "Submission list with status workflow and convert action"
      min_lines: 120
  key_links:
    - from: "components/admin/SubmissionsTab.tsx"
      to: "supabase.from('submissions')"
      via: "Fetch and update submissions"
      pattern: "from\\('submissions'\\)"
    - from: "components/admin/SubmissionsTab.tsx"
      to: "supabase.from('profiles')"
      via: "Insert new profile on convert action"
      pattern: "from\\('profiles'\\).insert"
---

<objective>
Build the Submissions tab with status workflow and convert-to-profile action.

Purpose: Allow admin to review membership form submissions and convert approved ones into live model profiles.
Output: Working Submissions tab with full status lifecycle and conversion flow.
</objective>

<execution_context>
@C:/Users/frenc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/frenc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-admin-cms/04-RESEARCH.md

<interfaces>
From lib/types.ts:
```typescript
export interface Submission {
  id: string;
  form_data: Record<string, any>;
  status: 'new' | 'reviewed' | 'approved' | 'dismissed' | 'converted';
  id_photo_url: string | null;
  created_at: string;
}
export interface Profile { id: string; name: string; slug: string | null; region: string | null; ... }
export interface FormConfig { title: string; fields: FormField[]; ... }
```

From lib/supabase/admin.ts:
```typescript
export function triggerRevalidation(paths: string[]): Promise<void>
export function generateSlug(name: string): string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SubmissionsTab with status workflow and convert action</name>
  <files>components/admin/SubmissionsTab.tsx</files>
  <action>
Create components/admin/SubmissionsTab.tsx matching prototype lines 1207-1298:

Props: { lang, formConfig: FormConfig, areas: string[] }

State: submissions (Submission[]), linkCopied (boolean), loading

On mount: Fetch all submissions ordered by created_at desc.

Header section:
- Title "Membership Submissions" + description
- "New" count badge (rose-soft bg, rose text)

Share Link Box (prototype lines 1218-1228):
- Left: "Share Membership Link" title + description
- Right: URL display (monospace, maxWidth 260, overflow ellipsis) showing window.location.origin + '/membership'
- Copy button: onClick copies URL to clipboard via navigator.clipboard.writeText, shows "Copied!" for 2 seconds

Empty state: "No submissions yet" with suggestion to share the membership form link.

Submission cards (grid, gap 12):
Each submission card:
1. Header row: avatar (first letter, colored by status), name, email, phone, submitted date, status badge
   - Status colors: { new: 'var(--rose)', reviewed: 'var(--peach)', approved: 'var(--sage)', dismissed: 'var(--sand)' }
   - Status badge: colored text on tinted background

2. Details section: 4-column grid showing Age, Height, Area, Experience from form_data
   - Shoot types as pill chips (border sand, text muted)
   - Bio text if present
   - Social/Instagram link if present (rose colored)
   - ID photo if present: thumbnail with "Uploaded" badge (check form_config fields for file_upload type fields, display matching form_data values)

3. Action buttons (borderTop, flex row):
   - Status transitions per prototype lines 1283-1293:
     - If new: "Mark Reviewed" (peach) and "Approve" (sage) and "Dismiss" (sand)
     - If reviewed: "Approve" (sage) and "Dismiss" (sand)
     - If approved: "Convert to Profile" (sage bg, dark text, full action)
   - Each status button: update submission via supabase.from('submissions').update({ status }).eq('id', submission.id)

Convert to Profile action (prototype lines 1287-1292):
- Map form_data fields to Profile columns:
  - name: form_data.name
  - region: form_data.region || areas[0] || 'LA'
  - parent_region: determine from region ('OC' if region contains 'OC', else 'LA')
  - bio: form_data.bio || ''
  - experience: form_data.exp || 'Beginner'
  - types: form_data.types || []
  - compensation: []
  - verified: false, vacation: false
  - attributes: { age: form_data.age, height: form_data.height }
  - slug: generateSlug(form_data.name)
  - profile_image: null, cover_image: null (admin uploads photos later)
- Insert into profiles table
- Update submission status to 'converted'
- Trigger revalidation: ['/']
- Refresh submissions list

Use useTranslation(lang) for all labels (subTitle, subDesc, subNew, subReviewed, subApproved, subDismissed, subMarkReviewed, subApprove, subDismiss, subConvert, etc.)
  </action>
  <verify>
    <automated>cd C:/Users/frenc/Projects/kpeachgirl-app && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
- Submissions tab lists all submissions with status badges
- Status transitions work: new -> reviewed -> approved -> dismissed
- Convert action creates a new profile from submission data
- Share link box shows membership URL with copy button
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire SubmissionsTab into AdminDashboard</name>
  <files>components/admin/AdminDashboard.tsx</files>
  <action>
Update AdminDashboard.tsx:
- Import SubmissionsTab
- When submissions tab is active, render SubmissionsTab with lang, formConfig (from site_config), and areas
- Update new submission count in nav: after SubmissionsTab mounts or submission status changes, refresh the count
- Pass a callback or use a shared state pattern so that when a submission is converted to profile, the models list is also marked as stale (if admin switches to Models tab, it should refetch)
  </action>
  <verify>
    <automated>cd C:/Users/frenc/Projects/kpeachgirl-app && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
- Submissions tab renders within AdminDashboard when tab is active
- New submission count in nav updates when statuses change
- Converting submission to profile triggers homepage revalidation
  </done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- Submissions tab displays submissions from database
- Status workflow buttons update submission status
- Convert action creates profile in profiles table
</verification>

<success_criteria>
- Admin can see all submissions with details and status
- Admin can advance submissions through the status workflow
- Admin can convert approved submissions into live model profiles
- Share link box provides copyable membership form URL
</success_criteria>

<output>
After completion, create `.planning/phases/04-admin-cms/04-07-SUMMARY.md`
</output>
