---
phase: 04-admin-cms
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - components/admin/ModelsTab.tsx
  - components/admin/ModelEditor.tsx
  - components/admin/AdminDashboard.tsx
autonomous: true
requirements:
  - ADM-05
  - ADM-06

must_haves:
  truths:
    - "Admin sees a list of all model profiles with name, area, level, verified/vacation toggles"
    - "Admin can filter models by All, LA, OC, Verified, Vacation"
    - "Admin can click Edit to open a slide-out editor panel"
    - "Admin can create a new model with all profile fields"
    - "Admin can edit any field (name, region, bio, experience, attributes, types, compensation, verified, vacation)"
    - "Admin can delete a model"
    - "Saving a model persists to Supabase profiles table"
  artifacts:
    - path: "components/admin/ModelsTab.tsx"
      provides: "Model list with stats cards, filters, and table"
      min_lines: 100
    - path: "components/admin/ModelEditor.tsx"
      provides: "Slide-out editor with all profile fields"
      min_lines: 150
  key_links:
    - from: "components/admin/ModelsTab.tsx"
      to: "supabase.from('profiles')"
      via: "Supabase query for model list"
      pattern: "from\\('profiles'\\)"
    - from: "components/admin/ModelEditor.tsx"
      to: "supabase.from('profiles')"
      via: "upsert for save, delete for removal"
      pattern: "upsert|delete"
---

<objective>
Build the Models tab with full CRUD: model list, stats, filters, and slide-out editor with all profile fields.

Purpose: Allow admin to manage all model profiles — the core content of the directory.
Output: Working Models tab with list view and slide-out editor for create/edit/delete.
</objective>

<execution_context>
@C:/Users/frenc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/frenc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-admin-cms/04-RESEARCH.md

<interfaces>
From lib/types.ts:
```typescript
export interface Profile {
  id: string; name: string; slug: string | null; region: string | null;
  parent_region: string | null; bio: string | null; types: string[];
  compensation: string[]; verified: boolean; vacation: boolean;
  experience: string | null; profile_image: string | null;
  profile_image_crop: CropData | null; cover_image: string | null;
  cover_image_crop: CropData | null; attributes: Record<string, string | undefined>;
  sort_order: number | null; created_at: string; updated_at: string;
}
export interface PillGroup { id: string; title: string; color: string; dataKey: string; options: string[]; }
export interface CategorySection { id: string; title: string; fields: CategoryField[]; }
export interface CategoryField { key: string; label: string; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ModelsTab with stats, filters, and model table</name>
  <files>components/admin/ModelsTab.tsx</files>
  <action>
Create components/admin/ModelsTab.tsx as 'use client' matching prototype lines 1040-1076:

Props: { lang, onEditModel }

State: models (Profile[]), filter ('all'|'la'|'oc'|'verified'|'vacation'), loading

On mount: Fetch all profiles from Supabase ordered by sort_order, then name. Use createClient() from lib/supabase/client.ts.

Stats bar (prototype lines 1045-1052): 4 cards in a row — Total models, Verified count, Groups count (fetch from groups table), On Vacation count. Each card: bg #181716, serif number (36px), muted label (10px uppercase).

Filter chips (prototype lines 1053-1057): All, LA, OC, Verified, Vacation. Active: bg charcoal, color cream. Inactive: bg #181716, border sand, color muted.

Model table (prototype lines 1058-1076): Each row shows:
- Thumbnail (32x32 profile_image or placeholder SVG)
- Name (bold 13px charcoal)
- Area/region (12px muted, hide on mobile)
- Level/experience (11px muted, hide on mobile)
- Verified toggle switch (sage green when on)
- Vacation toggle switch (peach when on)
- Edit button (charcoal bg, rose bg when editing)

Toggle switches: Clicking verified/vacation immediately updates Supabase profiles table with .update({ verified/vacation }).eq('id', model.id) and updates local state. No need to trigger ISR for toggle-only changes (saves are batched).

New Model button (top right, rose bg): Calls onEditModel(null) to open editor with empty form.

Use useTranslation(lang) for all labels.

Filter logic:
- 'la': parent_region === 'LA'
- 'oc': parent_region === 'OC'
- 'verified': verified === true
- 'vacation': vacation === true
  </action>
  <verify>
    <automated>cd C:/Users/frenc/Projects/kpeachgirl-app && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>Models tab displays all profiles in a table with stats, filters, toggle switches, and edit/new buttons</done>
</task>

<task type="auto">
  <name>Task 2: ModelEditor slide-out panel with all fields and CRUD</name>
  <files>components/admin/ModelEditor.tsx, components/admin/AdminDashboard.tsx</files>
  <action>
Create components/admin/ModelEditor.tsx as 'use client' matching prototype lines 1554-1673:

Props: { model: Profile | null, lang, categories: CategorySection[], pillGroups: PillGroup[], areas: string[], onSave, onDelete, onClose }

Structure — fixed position slide-out panel from right:
- Backdrop overlay (rgba(0,0,0,0.5), onClick closes)
- Panel: className "slide-in admin-slide" (responsive: 500px desktop, 420px tablet, full-width mobile via existing CSS classes)
- Sticky header: model thumbnail + name + "Editing" label + close button
- Scrollable content area (padding 20px 24px, paddingBottom 100px for sticky footer)
- Sticky footer: "Save and Publish" button + Cancel button

Form sections (matching prototype):
1. IDENTITY section (rose uppercase heading with border-bottom):
   - Name (text input)
   - Region (select from areas prop)
   - Parent Region (select: LA or OC)
   - Experience (select: Beginner/Intermediate/Experienced/Professional)

2. PHOTOS section: Placeholder for photo uploads (just empty boxes with "Photo upload available in next plan" text — actual upload wiring comes in plan 04-04)

3. Category sections: Loop through categories prop, render each with its fields as text inputs. Store field values in form state mapped to attributes jsonb + top-level fields.

4. BIOGRAPHY section: Textarea for bio field.

5. Tag pill groups: Loop through pillGroups prop, render each group's options as toggle buttons. Active: border + bg matching pill color. Inactive: border sand, bg #181716.

6. DELETE button at bottom: Red-tinted, hover fills rose.

Form state: Initialize from model prop (or empty for new). Map between Profile fields and form state:
- Top-level: name, region, parent_region, bio, experience, types, compensation, verified, vacation
- Attributes: category field values stored in attributes jsonb (e.g., attributes.age, attributes.height)

Save handler:
- Generate slug from name: toLowerCase().replace(/\s+/g, '-').replace(/[^a-z0-9-]/g, '')
- If new model (no id): supabase.from('profiles').insert({...}).select().single()
- If existing: supabase.from('profiles').update({...}).eq('id', model.id).select().single()
- Call onSave(savedProfile) to refresh parent list
- Show "Saved!" feedback briefly (swap button text to green for 2 seconds)

Delete handler: supabase.from('profiles').delete().eq('id', model.id), call onDelete(model.id)

Wire into AdminDashboard.tsx:
- Add state: editingModel (Profile | null | undefined — null=new, undefined=closed)
- Fetch site_config for categories, pill_groups, areas on mount
- When models tab is active, render ModelsTab with onEditModel callback
- When editingModel is not undefined, render ModelEditor overlay
- Pass onSave/onDelete/onClose handlers that refresh the models list
  </action>
  <verify>
    <automated>cd C:/Users/frenc/Projects/kpeachgirl-app && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
- Slide-out editor opens for new and existing models
- All profile fields editable (name, region, bio, experience, attributes, types, compensation)
- Save persists to Supabase profiles table
- Delete removes model from database
- Editor closes on save, delete, cancel, or backdrop click
  </done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- ModelsTab lists all profiles from database
- ModelEditor slide-out opens/closes properly
- CRUD operations work against Supabase profiles table
</verification>

<success_criteria>
- Models tab shows all profiles with stats, filters, toggle switches
- Clicking Edit opens slide-out editor with all fields populated
- New Model creates a profile in database
- Editing saves changes to database
- Deleting removes profile from database
</success_criteria>

<output>
After completion, create `.planning/phases/04-admin-cms/04-03-SUMMARY.md`
</output>
