---
phase: 03-public-pages
plan: 06
type: execute
wave: 3
depends_on: ["03-02", "03-03", "03-04", "03-05"]
files_modified:
  - next.config.mjs
autonomous: true
requirements: [PUB-19]

must_haves:
  truths:
    - "next/image loads Supabase Storage URLs without error"
    - "next/image loads Unsplash URLs without error (seed data)"
    - "Full production build succeeds with zero TypeScript errors"
    - "All routes render: /, /model/[slug], /group/[slug], /membership"
  artifacts:
    - path: "next.config.mjs"
      provides: "Image remotePatterns for both Supabase and Unsplash"
      contains: "images.unsplash.com"
  key_links:
    - from: "next.config.mjs"
      to: "next/image"
      via: "remotePatterns configuration"
      pattern: "remotePatterns"
---

<objective>
Configure next/image remotePatterns for Unsplash (seed data) and verify full build succeeds across all routes.

Purpose: Seed data uses Unsplash URLs which will fail without remotePatterns config. This is the final integration check.
Output: Working next.config.mjs and verified clean build.
</objective>

<execution_context>
@C:/Users/frenc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/frenc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-public-pages/03-RESEARCH.md
@next.config.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Unsplash to next/image remotePatterns and verify full build</name>
  <files>next.config.mjs</files>
  <action>
**Update next.config.mjs** to add images.unsplash.com to remotePatterns alongside existing Supabase pattern (Pitfall 4 from research):

```javascript
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: '*.supabase.co',
        pathname: '/storage/v1/object/public/**',
      },
      {
        protocol: 'https',
        hostname: 'images.unsplash.com',
      },
    ],
  },
}
```

Then run `npm run build` to verify the entire project builds cleanly:
- All TypeScript compiles
- All pages generate (homepage SSG, model/group pages via generateStaticParams)
- No missing imports or broken references

If build fails, fix all issues before completing this task. Common issues:
- Missing exports from lib files
- Type mismatches between Supabase data and TypeScript interfaces
- Import path errors
- Server/client component boundary violations
  </action>
  <verify>
    <automated>cd /c/Users/frenc/Projects/kpeachgirl-app && npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>next.config.mjs has both Supabase and Unsplash remotePatterns. `npm run build` succeeds with zero errors. All routes generate correctly.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with zero errors
- Build output shows all routes generated: /, /model/[slug] (12 pages), /group/[slug], /membership
- next.config.mjs includes both Supabase and Unsplash hostnames
</verification>

<success_criteria>
- Production build succeeds cleanly
- All 4 route patterns render without error
- Unsplash seed images load through next/image
- No TypeScript errors in the build
</success_criteria>

<output>
After completion, create `.planning/phases/03-public-pages/03-06-SUMMARY.md`
</output>
