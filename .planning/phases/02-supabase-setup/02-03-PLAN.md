---
phase: 02-supabase-setup
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - supabase/seed.sql
autonomous: false
requirements: [DB-15, DB-16]

must_haves:
  truths:
    - "12 seed model profiles exist in the profiles table with all attributes"
    - "Gallery images exist for each of the 12 models"
    - "6 site_config rows exist (areas, categories, card_settings, pill_groups, hero, form_config)"
    - "Seed data matches prototype ModelDirectory.jsx exactly"
  artifacts:
    - path: "supabase/seed.sql"
      provides: "Complete seed data for profiles, gallery_images, and site_config"
      contains: "INSERT INTO profiles"
  key_links:
    - from: "supabase/seed.sql"
      to: "supabase/migrations/20260309000001_create_tables.sql"
      via: "INSERTs into tables created by migration"
      pattern: "INSERT INTO profiles"
    - from: "supabase/seed.sql"
      to: "ModelDirectory.jsx"
      via: "Data extracted from prototype MODELS array"
      pattern: "Aria Novak"
---

<objective>
Create the seed.sql file with all 12 model profiles from the prototype, their gallery images, and all 6 site_config default values. Then run all migrations and seed in Supabase Dashboard.

Purpose: Seed data populates the directory so public pages have content from day one. All data comes from the working prototype.
Output: supabase/seed.sql ready to run + live database populated
</objective>

<execution_context>
@C:/Users/frenc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/frenc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-supabase-setup/02-RESEARCH.md
@CLAUDE.md
@ModelDirectory.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create seed.sql with all prototype data</name>
  <files>supabase/seed.sql</files>
  <action>
Create `supabase/seed.sql` with all seed data from the ModelDirectory.jsx prototype. Use gen_random_uuid() for IDs (omit id column in INSERTs). Make the script idempotent using TRUNCATE + INSERT pattern with a transaction wrapper.

**Section 1: Truncate existing data (for re-runnability)**
```sql
BEGIN;
TRUNCATE gallery_images, group_gallery_images, groups, profiles, submissions CASCADE;
DELETE FROM site_config;
```

**Section 2: Insert all 12 profiles**

Insert each of the 12 models from the MODELS array in ModelDirectory.jsx. For each model, map fields as follows:
- name, slug (lowercase hyphenated from name, e.g., "Aria Novak" -> "aria-novak"), region, parent_region (from parentRegion), bio, types (as ARRAY['Fashion','Editorial',...]), compensation (as ARRAY[...]), verified, vacation, experience (from exp field), profile_image (from img field), attributes (jsonb with age, height, weight, bust, waist, hips, hair, eyes, shoe, dress, tattoos, piercings, instagram), sort_order (1-12)

The 12 models in order:
1. Aria Novak — LA, Professional, verified, not on vacation
2. Jordan Reyes — West LA, Experienced, verified
3. Luna Chen — OC, Intermediate, verified
4. Mia Rivera — OC, Beginner, not verified
5. Sasha Okonkwo — LA, Professional, verified, ON VACATION (vacation=true)
6. Kaia Tanaka — Mid-Wilshire, Experienced, verified
7. Valentina Rossi — OC, Professional, verified
8. Demi Moreno — Mid-Wilshire, Intermediate, not verified
9. Priya Sharma — West LA, Professional, verified
10. Elena Brooks — LA, Experienced, verified
11. Camille Dubois — Mid-Wilshire, Professional, verified
12. Naomi Vasquez — OC, Beginner, not verified

Use the exact Unsplash URLs from the prototype for profile_image and cover_image. Put all measurement/attribute fields in the attributes jsonb column.

Important: profile_image_crop and cover_image_crop should be NULL (no crop data in prototype seed).

**Section 3: Insert gallery images**

After profile inserts, use a CTE or subquery to look up each profile's UUID by slug, then insert gallery image rows. Each model has 5-8 gallery images from the prototype. Use the exact Unsplash URLs. Set sort_order starting at 1 for each profile.

Pattern:
```sql
INSERT INTO gallery_images (profile_id, url, sort_order)
SELECT p.id, urls.url, urls.sort_order
FROM profiles p
CROSS JOIN (VALUES
  ('https://images.unsplash.com/photo-...', 1),
  ('https://images.unsplash.com/photo-...', 2)
) AS urls(url, sort_order)
WHERE p.slug = 'aria-novak';
```

Repeat for all 12 models with their exact gallery URLs from the prototype.

**Section 4: Insert site_config rows**

Insert 6 rows with ON CONFLICT (id) DO UPDATE for idempotency:

1. 'areas' — `["LA","West LA","Mid-Wilshire","OC"]`

2. 'categories' — Full JSON array per CLAUDE.md:
```json
[
  {"id":"stats","title":"Vitals","fields":[
    {"key":"age","label":"Age"},{"key":"height","label":"Height"},{"key":"weight","label":"Weight"},
    {"key":"bust","label":"Bust"},{"key":"waist","label":"Waist"},{"key":"hips","label":"Hips"}
  ]},
  {"id":"appearance","title":"Look","fields":[
    {"key":"hair","label":"Hair"},{"key":"eyes","label":"Eyes"},{"key":"shoe","label":"Shoe"},
    {"key":"dress","label":"Dress"},{"key":"tattoos","label":"Tattoos"},{"key":"piercings","label":"Piercings"}
  ]},
  {"id":"professional","title":"Work","fields":[
    {"key":"exp","label":"Experience"},{"key":"region","label":"Region"}
  ]}
]
```

3. 'card_settings' — Per CLAUDE.md:
```json
{"subtitleFields":["region","types"],"showVerifiedBadge":true,"showAwayBadge":true,"verifiedLabel":"Verified","awayLabel":"Away","overlayColor":"#1a1a1a","overlayOpacity":70}
```

4. 'pill_groups' — Per CLAUDE.md:
```json
[
  {"id":"types","title":"Shoot Types","color":"var(--charcoal)","dataKey":"types","options":["Portrait","Fashion","Commercial","Glamour","Fitness","Editorial","Artistic","Swimwear","Lingerie","Cosplay","Lifestyle","Event"]},
  {"id":"compensation","title":"Compensation","color":"var(--sage)","dataKey":"compensation","options":["Paid Only","TFP","Negotiable"]}
]
```

5. 'hero' — Per CLAUDE.md:
```json
{"img":"","imgCrop":null,"subtitle":"Los Angeles \u00b7 Orange County","titleLine1":"Find Your","titleLine2":"Perfect","titleAccent":"Model","searchPlaceholder":"Search by name or area..."}
```

6. 'form_config' — Per CLAUDE.md, full form configuration with title, subtitle, successTitle, successMsg, submitLabel, and all 11 fields array.

End with `COMMIT;`
  </action>
  <verify>
    <automated>grep -c "INSERT INTO profiles" supabase/seed.sql | grep -q "1" && grep -c "INSERT INTO gallery_images" supabase/seed.sql && grep -c "INSERT INTO site_config" supabase/seed.sql && grep -q "aria-novak" supabase/seed.sql && grep -q "naomi-vasquez" supabase/seed.sql && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>seed.sql contains all 12 profiles with correct attributes, gallery images for each model, and all 6 site_config rows. Data matches prototype exactly. Script is idempotent (TRUNCATE + re-insert within transaction).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Run migrations and seed data in Supabase Dashboard</name>
  <files>supabase/verify.sql</files>
  <action>
Human-only action: Run all SQL migration files and seed data in the Supabase Dashboard SQL Editor. Also enable the Custom Access Token Hook in Dashboard settings. These steps require Supabase Dashboard access.

Steps for the user:
1. Open Supabase Dashboard > SQL Editor
2. Run migrations IN ORDER (copy-paste each file):
   a. supabase/migrations/20260309000001_create_tables.sql
   b. supabase/migrations/20260309000002_enable_rls.sql
   c. supabase/migrations/20260309000003_storage_buckets.sql
   d. supabase/migrations/20260309000004_custom_access_token_hook.sql
3. Run supabase/seed.sql
4. Run supabase/verify.sql and check output matches expectations
5. Go to Authentication > Hooks (Beta) > Enable Custom Access Token (JWT) hook > select public.custom_access_token_hook > Save
6. Verify: SELECT count(*) FROM profiles; (expect 12) and SELECT id FROM site_config; (expect 6 rows)
  </action>
  <verify>
    <automated>echo "Manual verification required: user confirms migrations ran in Supabase Dashboard"</automated>
  </verify>
  <done>All 4 migrations executed successfully. Seed data loaded: 12 profiles, gallery images, 6 site_config rows. Custom Access Token Hook enabled in Dashboard. verify.sql output confirms all tables, policies, buckets, and hook function exist.</done>
</task>

</tasks>

<verification>
- supabase/seed.sql exists with 12 profile INSERTs + gallery images + 6 site_config rows
- After running in Dashboard: SELECT count(*) FROM profiles returns 12
- SELECT id FROM site_config returns 6 rows (areas, categories, card_settings, pill_groups, hero, form_config)
- SELECT count(*) FROM gallery_images returns ~75 rows (sum of all model galleries)
</verification>

<success_criteria>
- 12 model profiles in database matching prototype data exactly
- Gallery images linked to correct profiles via profile_id
- All 6 site_config rows present with correct JSON values
- Seed script is re-runnable (idempotent via TRUNCATE + transaction)
</success_criteria>

<output>
After completion, create `.planning/phases/02-supabase-setup/02-03-SUMMARY.md`
</output>
