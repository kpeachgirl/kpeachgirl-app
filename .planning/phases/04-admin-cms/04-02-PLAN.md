---
phase: 04-admin-cms
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - components/admin/AdminDashboard.tsx
  - components/admin/AdminNav.tsx
  - app/admin/page.tsx
autonomous: true
requirements:
  - ADM-04

must_haves:
  truths:
    - "Admin sees 5 tabs: Models, Groups, Submissions, Profile Fields, Areas"
    - "Clicking a tab switches the visible content panel"
    - "EN/Korean language toggle switches all admin labels instantly"
    - "Logout button signs out and redirects to /admin/login"
    - "Admin email avatar and email shown in nav bar"
  artifacts:
    - path: "components/admin/AdminDashboard.tsx"
      provides: "Main dashboard shell with tab state and language toggle"
      min_lines: 60
    - path: "components/admin/AdminNav.tsx"
      provides: "Top navigation bar with tabs, lang toggle, user info, logout"
      min_lines: 50
  key_links:
    - from: "app/admin/page.tsx"
      to: "components/admin/AdminDashboard.tsx"
      via: "renders AdminDashboard"
      pattern: "AdminDashboard"
    - from: "components/admin/AdminDashboard.tsx"
      to: "components/admin/AdminNav.tsx"
      via: "renders AdminNav with tab/lang state"
      pattern: "AdminNav"
    - from: "components/admin/AdminNav.tsx"
      to: "lib/supabase/client.ts"
      via: "signOut for logout"
      pattern: "signOut"
---

<objective>
Build the admin dashboard shell with 5-tab navigation, EN/Korean language toggle, and logout.

Purpose: Provide the structural container that all admin tab content will render inside.
Output: Working admin dashboard at /admin with tab switching, language toggle, and logout button.
</objective>

<execution_context>
@C:/Users/frenc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/frenc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-admin-cms/04-RESEARCH.md

<interfaces>
From lib/types.ts:
```typescript
export interface Profile { id: string; name: string; slug: string | null; ... }
export interface Group { id: string; name: string; slug: string | null; ... }
export interface Submission { id: string; form_data: Record<string, any>; status: 'new' | 'reviewed' | 'approved' | 'dismissed' | 'converted'; ... }
export interface SiteConfig { id: string; value: any; updated_at: string; }
```

From lib/i18n.ts:
```typescript
export function useTranslation(lang: 'en' | 'ko'): Record<string, string>
```

From lib/supabase/client.ts:
```typescript
export function createClient(): SupabaseClient
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AdminNav component with tabs, language toggle, user info, logout</name>
  <files>components/admin/AdminNav.tsx</files>
  <action>
Create components/admin/AdminNav.tsx as a 'use client' component matching prototype lines 1006-1037:

Props: { activeTab, onTabChange, lang, onLangChange, userEmail, onLogout }

Structure (dark nav bar, borderBottom 1px solid var(--sand), bg #181716):
- Left side: "Back to Site" link (next/link to /), Kpeachgirl serif logo, "ADMIN" rose badge
- Center: 5 tab buttons in a row — models, groups, submissions, categories (labeled "Profile Fields"), areas
  - Active tab: bg var(--charcoal), color var(--cream)
  - Inactive: bg transparent, color var(--muted)
  - Submissions tab shows red badge with count of "new" submissions (pass as prop: newSubmissionCount)
  - All labels use translations from i18n (t.tabModels, t.tabGroups, t.tabSubmissions, t.tabCategories, t.tabAreas)
  - Font: 11px, weight 700, letter-spacing 0.08em, uppercase
- Right side:
  - Language toggle: EN/Korean buttons in a pill group (bg charcoal when active, transparent when not)
  - User avatar: 28x28 rose-soft bg with first letter of email, email text next to it (hide on mobile via className mob-hide)
  - Logout button: border 1px solid var(--sand), uppercase, hover changes border+color to rose
  - Logout calls supabase.auth.signOut() then router.push('/admin/login') + router.refresh()

Responsive: Use className "admin-nav-pad" for padding, "admin-tabs" for tab container. Tabs should wrap on smaller screens (flexWrap: wrap, gap: 8).
  </action>
  <verify>
    <automated>cd C:/Users/frenc/Projects/kpeachgirl-app && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>AdminNav renders tabs, language toggle, user info, logout button with prototype-matching styling</done>
</task>

<task type="auto">
  <name>Task 2: AdminDashboard shell and admin page wiring</name>
  <files>components/admin/AdminDashboard.tsx, app/admin/page.tsx</files>
  <action>
Create components/admin/AdminDashboard.tsx as 'use client' component:
- State: activeTab (default 'models'), lang (default 'en')
- On mount: fetch user email from supabase.auth.getUser() to display in nav
- On mount: fetch submission count (new status) for nav badge: supabase.from('submissions').select('id', { count: 'exact' }).eq('status', 'new')
- Render AdminNav with all state handlers
- Below nav: content area (maxWidth 1200, margin auto, padding 32px 24px)
- Tab content area renders placeholder divs for each tab with tab name text — these will be replaced by actual tab components in plans 04-03 through 04-08
- Use useTranslation(lang) from lib/i18n.ts for all text

Update app/admin/page.tsx:
- Make it 'use client'
- Import and render AdminDashboard
- Add className "sans grain" and minHeight 100vh, bg var(--cream) wrapper
  </action>
  <verify>
    <automated>cd C:/Users/frenc/Projects/kpeachgirl-app && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
- /admin renders dashboard shell with 5 working tabs
- Language toggle switches between EN and Korean
- Logout button signs out and redirects to login
- Submissions badge shows count of new submissions
  </done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- /admin page renders AdminDashboard with 5 tabs
- Tab switching works (client-side state)
- Language toggle changes labels
</verification>

<success_criteria>
- Admin dashboard shell at /admin with all 5 tabs clickable
- EN/Korean toggle instantly switches all labels
- Logout clears session and redirects to /admin/login
- New submission count badge visible on Submissions tab
</success_criteria>

<output>
After completion, create `.planning/phases/04-admin-cms/04-02-SUMMARY.md`
</output>
