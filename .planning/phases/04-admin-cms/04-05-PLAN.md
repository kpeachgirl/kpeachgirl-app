---
phase: 04-admin-cms
plan: 05
type: execute
wave: 4
depends_on: ["04-03", "04-04"]
files_modified:
  - components/admin/ModelEditor.tsx
  - components/admin/AdminDashboard.tsx
autonomous: true
requirements:
  - ADM-07
  - ADM-08

must_haves:
  truths:
    - "Admin can upload profile photo via click-to-upload in the model editor"
    - "Admin can upload cover photo via click-to-upload in the model editor"
    - "Admin can open PhotoEditor to set focal point and zoom on profile/cover photos"
    - "Admin can upload multiple gallery images"
    - "Admin can delete individual gallery images"
    - "Gallery images are stored in gallery_images table linked to the profile"
    - "Crop data is persisted to profile_image_crop, cover_image_crop, and gallery_images.crop columns"
  artifacts:
    - path: "components/admin/ModelEditor.tsx"
      provides: "Model editor with full photo upload + crop integration"
      min_lines: 200
  key_links:
    - from: "components/admin/ModelEditor.tsx"
      to: "lib/supabase/admin.ts"
      via: "uploadImage for all photo uploads"
      pattern: "uploadImage"
    - from: "components/admin/ModelEditor.tsx"
      to: "components/admin/PhotoEditor.tsx"
      via: "opens PhotoEditor modal for crop editing"
      pattern: "PhotoEditor"
    - from: "components/admin/ModelEditor.tsx"
      to: "supabase.from('gallery_images')"
      via: "insert/delete gallery image rows"
      pattern: "gallery_images"
---

<objective>
Wire photo uploads and PhotoEditor crop into the ModelEditor slide-out panel.

Purpose: Complete the model editing experience with full image management — the visual core of the directory.
Output: ModelEditor with working profile photo, cover photo, and gallery image management.
</objective>

<execution_context>
@C:/Users/frenc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/frenc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-admin-cms/04-RESEARCH.md
@.planning/phases/04-admin-cms/04-03-SUMMARY.md
@.planning/phases/04-admin-cms/04-04-SUMMARY.md

<interfaces>
From lib/supabase/admin.ts:
```typescript
export function uploadImage(file: File, bucket: string, folder: string): Promise<string>
export function deleteStorageFile(bucket: string, path: string): Promise<void>
export function triggerRevalidation(paths: string[]): Promise<void>
```

From components/admin/PhotoEditor.tsx:
```typescript
// Props: { src, crop, aspect, onSave, onCancel, translations }
```

From lib/types.ts:
```typescript
export interface CropData { x: number; y: number; zoom: number; }
export interface GalleryImage { id: string; profile_id: string; url: string; crop: CropData | null; sort_order: number | null; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire photo uploads into ModelEditor photos section</name>
  <files>components/admin/ModelEditor.tsx</files>
  <action>
Update the ModelEditor PHOTOS section (replacing placeholder from plan 04-03) to match prototype lines 1580-1637:

Profile photo upload area (aspect 2/3):
- If image exists: show image with crop applied (objectPosition + transform scale), overlay buttons "Edit" (opens PhotoEditor) and "Upload" (opens file picker), X button to remove
- If no image: dashed border area, "Click to upload" text, opens file picker on click
- Hidden file input: accept="image/*", on change:
  1. Call uploadImage(file, 'profile-images', profileId || 'new') from lib/supabase/admin.ts
  2. Update form state with returned public URL
  3. Reset input value (e.target.value = '') per research pitfall #5

Cover photo upload area (aspect 7/3): Same pattern as profile photo, uses 'cover-images' bucket.

PhotoEditor integration:
- State: editingPhoto: { key: 'profile_image' | 'cover_image' | 'gallery', idx?: number } | null
- When editingPhoto is set, render PhotoEditor modal with correct src, crop, aspect
- Profile photo: aspect '2/3', crop from profile_image_crop
- Cover photo: aspect '7/3', crop from cover_image_crop
- Gallery: aspect '3/4', crop from gallery image's crop field
- On PhotoEditor save: update the corresponding crop in form state

Gallery section (prototype lines 1616-1637):
- Grid of existing gallery images (3 columns, gap 6, aspect 3/4)
- Each image: shows image with crop applied, X delete button, Edit crop button, index number
- "+" add button at end: opens file picker for new gallery image
- On gallery upload: call uploadImage(file, 'gallery-images', profileId)
- Insert into gallery_images table: supabase.from('gallery_images').insert({ profile_id, url, sort_order })
- On gallery delete: supabase.from('gallery_images').delete().eq('id', imageId), also call deleteStorageFile
- Load gallery images on editor open: supabase.from('gallery_images').select('*').eq('profile_id', model.id).order('sort_order')

For new models (no id yet): Upload profile/cover images immediately, store URLs. Gallery images can only be added after initial save (need profile_id). Show message "Save the model first to add gallery images" if model is new.
  </action>
  <verify>
    <automated>cd C:/Users/frenc/Projects/kpeachgirl-app && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
- Profile and cover photo upload works via click-to-upload
- PhotoEditor opens for focal point + zoom on all photos
- Gallery images can be uploaded, viewed with crop, and deleted
- Crop data persists to correct database columns
- File input resets after each upload
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire ISR revalidation into model save/delete</name>
  <files>components/admin/ModelEditor.tsx, components/admin/ModelsTab.tsx</files>
  <action>
Update ModelEditor save handler:
- After successful Supabase upsert, call triggerRevalidation(['/', `/model/${slug}`])
- If model is in any groups, also revalidate those group pages: fetch groups where member_ids contains this profile id, revalidate `/group/${group.slug}`

Update ModelEditor delete handler:
- After successful delete, call triggerRevalidation(['/', `/model/${model.slug}`])
- Also delete all gallery_images for this profile
- Also delete storage files for profile_image, cover_image if they exist

Update ModelsTab toggle handlers (verified/vacation):
- After toggle update, call triggerRevalidation(['/', `/model/${model.slug}`])

Update AdminDashboard to pass a refreshModels callback to ModelsTab so the list refreshes after editor save/delete.
  </action>
  <verify>
    <automated>cd C:/Users/frenc/Projects/kpeachgirl-app && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
- Model save triggers ISR revalidation for homepage and model profile page
- Model delete cleans up gallery images, storage files, and triggers revalidation
- Toggle switches trigger revalidation for affected pages
  </done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- Image upload uses UUID-based paths via uploadImage helper
- PhotoEditor opens from profile, cover, and gallery image edit buttons
- ISR revalidation fires on save, delete, and toggle operations
</verification>

<success_criteria>
- Admin can upload and crop profile photo, cover photo, and gallery images
- PhotoEditor modal works for all image types with correct aspect ratios
- Gallery images persist to gallery_images table with sort_order
- All model mutations trigger ISR revalidation of affected pages
</success_criteria>

<output>
After completion, create `.planning/phases/04-admin-cms/04-05-SUMMARY.md`
</output>
