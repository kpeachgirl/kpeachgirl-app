---
phase: 04-admin-cms
plan: 09
type: execute
wave: 4
depends_on: ["04-02"]
files_modified:
  - components/admin/AreasTab.tsx
  - app/api/contact/route.ts
  - app/api/revalidate/route.ts
  - components/admin/AdminDashboard.tsx
  - package.json
autonomous: true
requirements:
  - ADM-18
  - ADM-19
  - API-01
  - API-02
user_setup:
  - service: resend
    why: "Email sending for contact form"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard -> API Keys (https://resend.com/api-keys)"
    dashboard_config: []

must_haves:
  truths:
    - "Admin can add and remove geographic areas used for filtering"
    - "/api/contact POST endpoint sends email via Resend"
    - "/api/revalidate POST endpoint validates secret and calls revalidatePath"
    - "All admin saves trigger /api/revalidate to refresh affected public pages"
  artifacts:
    - path: "components/admin/AreasTab.tsx"
      provides: "Areas management: add/remove areas"
      min_lines: 40
    - path: "app/api/contact/route.ts"
      provides: "Contact form email route via Resend"
      min_lines: 20
    - path: "app/api/revalidate/route.ts"
      provides: "ISR revalidation endpoint"
      min_lines: 15
  key_links:
    - from: "app/api/revalidate/route.ts"
      to: "next/cache"
      via: "revalidatePath import"
      pattern: "revalidatePath"
    - from: "app/api/contact/route.ts"
      to: "resend"
      via: "Resend SDK for email sending"
      pattern: "Resend"
    - from: "lib/supabase/admin.ts"
      to: "app/api/revalidate/route.ts"
      via: "triggerRevalidation POST call"
      pattern: "/api/revalidate"
---

<objective>
Build the Areas tab, /api/contact email route, and /api/revalidate ISR endpoint.

Purpose: Complete the remaining admin tab and the two API routes that support the admin workflow.
Output: Working Areas management, contact email delivery, and on-demand ISR revalidation.
</objective>

<execution_context>
@C:/Users/frenc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/frenc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-admin-cms/04-RESEARCH.md

<interfaces>
From lib/supabase/admin.ts:
```typescript
export function triggerRevalidation(paths: string[]): Promise<void>
```

ENV vars needed:
- RESEND_API_KEY (for /api/contact)
- ADMIN_EMAIL (recipient for contact form)
- REVALIDATION_SECRET (for /api/revalidate external calls)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AreasTab component</name>
  <files>components/admin/AreasTab.tsx, components/admin/AdminDashboard.tsx</files>
  <action>
Create components/admin/AreasTab.tsx matching prototype lines 1534-1551:

Props: { lang, areas: string[], onUpdate: (areas: string[]) => void }

State: newArea (string)

Structure (bg #181716, border sand, padding 28):
- Title "Geographic Areas" (serif) + description "Manage the areas available for model filtering"
- Area chips: flex wrap, gap 8. Each chip shows area name (13px bold charcoal) + X remove button (22x22, border sand)
  - Remove: filter area out, call onUpdate with new array
- Add area input + button row:
  - Input: text input for new area name, Enter key triggers add
  - Add button: bg charcoal (or sand if empty), uppercase
  - Add logic: trim, check not empty, check not duplicate, add to array, call onUpdate, clear input

Wire into AdminDashboard.tsx:
- When areas tab active, render AreasTab
- onUpdate handler: supabase.from('site_config').update({ value: newAreas }).eq('id', 'areas'), then triggerRevalidation(['/'])
  </action>
  <verify>
    <automated>cd C:/Users/frenc/Projects/kpeachgirl-app && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>Areas tab displays current areas, admin can add new and remove existing, changes persist to site_config</done>
</task>

<task type="auto">
  <name>Task 2: API routes — /api/contact (Resend) and /api/revalidate (ISR)</name>
  <files>app/api/contact/route.ts, app/api/revalidate/route.ts, package.json</files>
  <action>
Install resend: `npm install resend`

Create app/api/contact/route.ts matching research code example:
- Export async POST handler
- Parse JSON body: { name, email, message, modelName }
- Validate required fields (name, email, message), return 400 if missing
- Create Resend instance with process.env.RESEND_API_KEY
- Send email:
  - from: 'Kpeachgirl <onboarding@resend.dev>' (dev default; production needs verified domain)
  - to: [process.env.ADMIN_EMAIL!]
  - subject: `Contact: ${modelName} - from ${name}`
  - text: `Name: ${name}\nEmail: ${email}\n\nMessage:\n${message}`
- On error: return 500 with { error: 'Failed to send' }
- On success: return 200 with { success: true }

Create app/api/revalidate/route.ts:
- Export async POST handler
- Check for x-revalidation-secret header:
  - If present and doesn't match process.env.REVALIDATION_SECRET, return 401 { error: 'Invalid secret' }
  - If absent (same-origin admin calls), allow through (no secret needed for internal use)
- Parse JSON body: { paths: string[] }
- Validate paths is an array
- For each path: call revalidatePath(path) from 'next/cache'
- Return 200 with { revalidated: true, paths }
- Wrap in try/catch, return 500 on error

Also wire the contact form button on the model profile page: Update the placeholder contact button in components/ModelProfileClient.tsx (noted as placeholder in Phase 3 decision [03-03]) to open a mailto or trigger the contact API. Use a simple approach: make the button open a modal with name/email/message fields that POSTs to /api/contact. If this is too much scope, at minimum make the button call `/api/contact` with a simple form — or leave the existing placeholder with a TODO comment for now and handle in a gap closure if needed. The minimal requirement is the API route itself exists and works.
  </action>
  <verify>
    <automated>cd C:/Users/frenc/Projects/kpeachgirl-app && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
- /api/contact POST sends email via Resend SDK
- /api/revalidate POST validates secret and calls revalidatePath
- resend package installed in package.json
- Both routes return proper error responses for invalid input
  </done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- app/api/contact/route.ts exists and exports POST
- app/api/revalidate/route.ts exists and exports POST
- resend listed in package.json dependencies
</verification>

<success_criteria>
- Areas tab allows adding/removing geographic areas with persistence
- Contact API route sends email via Resend with proper error handling
- Revalidation API route refreshes ISR pages with secret validation
- Admin saves across all tabs trigger revalidation
</success_criteria>

<output>
After completion, create `.planning/phases/04-admin-cms/04-09-SUMMARY.md`
</output>
