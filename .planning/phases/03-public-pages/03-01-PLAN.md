---
phase: 03-public-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/types.ts
  - lib/constants.ts
  - lib/i18n.ts
  - lib/utils.ts
  - components/AgeGate.tsx
  - components/ModelCard.tsx
  - components/GroupCard.tsx
  - components/PhotoCropDisplay.tsx
  - components/Lightbox.tsx
  - components/Navbar.tsx
  - components/Footer.tsx
  - components/FilterBar.tsx
  - components/ProfilePills.tsx
  - components/CategoryStats.tsx
autonomous: true
requirements: [PUB-01, PUB-06, PUB-07, PUB-08]

must_haves:
  truths:
    - "TypeScript interfaces for Profile, Group, Submission, SiteConfig, GalleryImage are importable from lib/types"
    - "Default config values for all 6 site_config keys are importable from lib/constants"
    - "i18n hook returns correct EN and Korean strings for 70+ keys"
    - "AgeGate blocks content until 18+ confirmed via sessionStorage"
    - "ModelCard renders profile image with crop, name, subtitle, verified/away badges"
    - "GroupCard renders group image with DUO/TRIO/GROUP badge"
  artifacts:
    - path: "lib/types.ts"
      provides: "All TypeScript interfaces"
      exports: ["Profile", "Group", "Submission", "SiteConfig", "GalleryImage", "CardSettings", "PillGroup", "HeroConfig", "FormConfig", "CropData", "CategorySection"]
    - path: "lib/constants.ts"
      provides: "Default site_config values"
      exports: ["DEFAULT_AREAS", "DEFAULT_CARD_SETTINGS", "DEFAULT_PILL_GROUPS", "DEFAULT_HERO", "DEFAULT_FORM_CONFIG", "DEFAULT_CATEGORIES"]
    - path: "lib/i18n.ts"
      provides: "Translation keys and useTranslation hook"
      exports: ["useTranslation", "translations"]
    - path: "lib/utils.ts"
      provides: "cn helper for class merging"
      exports: ["cn", "cropStyle"]
    - path: "components/AgeGate.tsx"
      provides: "Age verification overlay"
    - path: "components/ModelCard.tsx"
      provides: "Model card for grid display"
    - path: "components/GroupCard.tsx"
      provides: "Group card for grid display"
  key_links:
    - from: "components/ModelCard.tsx"
      to: "lib/types.ts"
      via: "import Profile, CardSettings"
      pattern: "import.*Profile.*from.*lib/types"
    - from: "components/AgeGate.tsx"
      to: "sessionStorage"
      via: "getItem/setItem age_verified"
      pattern: "sessionStorage"
---

<objective>
Create all shared library files (types, constants, i18n, utils) and reusable UI components that every public page depends on.

Purpose: Every subsequent plan imports from these files. Without them, nothing compiles.
Output: lib/types.ts, lib/constants.ts, lib/i18n.ts, lib/utils.ts, and 10 shared components in components/
</objective>

<execution_context>
@C:/Users/frenc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/frenc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-public-pages/03-RESEARCH.md
@ModelDirectory.jsx (lines 1-75 for defaults, 348-475 for AgeGate/ModelCard/ProfilePage components)
@app/layout.tsx
@app/globals.css
@lib/supabase/server.ts
@lib/supabase/client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared library files (types, constants, i18n, utils)</name>
  <files>lib/types.ts, lib/constants.ts, lib/i18n.ts, lib/utils.ts</files>
  <action>
Create 4 lib files completing FOUND-05, FOUND-06, FOUND-07:

**lib/types.ts** -- TypeScript interfaces matching Supabase schema exactly:
- `CropData`: `{ x: number; y: number; zoom: number }`
- `Profile`: All columns from profiles table. NOTE: `attributes` is `Record<string, string | undefined>` (jsonb). Fields like age, height, hair are in `attributes`, NOT top-level. Include `profile_image_crop` and `cover_image_crop` as `CropData | null`.
- `GalleryImage`: id, profile_id, url, crop (CropData | null), sort_order, created_at
- `Group`: All columns from groups table. `member_ids` is `string[]` (uuid array). Include `attributes` jsonb same as Profile.
- `GroupGalleryImage`: id, group_id, url, sort_order, created_at
- `Submission`: id, form_data (Record<string, any>), status ('new'|'reviewed'|'approved'|'dismissed'|'converted'), id_photo_url, created_at
- `SiteConfig`: id (string), value (any), updated_at
- `CardSettings`: subtitleFields, showVerifiedBadge, showAwayBadge, verifiedLabel, awayLabel, overlayColor, overlayOpacity -- match DEFAULT_CARD_SETTINGS from prototype
- `PillGroup`: id, title, color, dataKey, options (string[])
- `HeroConfig`: img, imgCrop (CropData|null), subtitle, titleLine1, titleLine2, titleAccent, searchPlaceholder
- `FormConfig`: title, subtitle, successTitle, successMsg, submitLabel, fields (FormField[])
- `FormField`: id, label, type ('text'|'email'|'textarea'|'area_select'|'exp_select'|'type_pills'|'file_upload'), required (boolean), width ('full'|'half'|'third'), placeholder, helperText?
- `CategorySection`: id, title, fields ({key: string, label: string}[])
- `AreaConfig`: string[] (just an array of area name strings)

**lib/constants.ts** -- Export all 6 default config objects matching prototype lines 10-73 exactly:
- DEFAULT_AREAS, DEFAULT_CARD_SETTINGS, DEFAULT_PILL_GROUPS, DEFAULT_HERO, DEFAULT_FORM_CONFIG, DEFAULT_CATEGORIES
- Import and use types from lib/types.ts for type safety

**lib/i18n.ts** -- Translation system:
- Export `translations` object with `en` and `ko` keys
- Include 70+ keys from prototype's `T` object (lines 818-880 in ModelDirectory.jsx). Key groups: navigation (back, admin), tabs (tabModels, tabCategories, tabAreas, tabGroups), model management (total, verified, onVacation, pending, newModel, editModel, deleteModel, fields), group management (grpTitle, grpDesc, grpNew, grpName, grpBio, grpMembers, grpGallery, grpBadgeLabel, grpDelete, grpNone), submissions (tabSubmissions, subTitle, subNew, subReviewed, subApproved, subDismissed, subConverted, subConvert, subNoSubs, subDeleteConfirm), profile fields (heroTitle, heroSubtitle, heroAccent, heroImg, cardTitle, cardSubtitle, cardVerified, cardAway, cardOverlay, tagGroupTitle, tagTitle, tagColor, tagOptions, formEditorTitle, catTitle, catSection, catFieldKey, catFieldLabel), common (save, cancel, delete, confirm, search, upload, remove, close, apply, reset), public-facing (ageTitle, ageBody, ageEnter, ageLeave, ageDisclaimer, contactBtn, portfolio, alsoAvailableAs, membershipTitle, copyright, noResults, area, verifiedFilter, availableFilter, modelsCount)
- Export `useTranslation` hook: `(lang: 'en' | 'ko') => Record<string, string>` that returns the translations for the given language. Simple implementation: return translations[lang].
- Korean translations should be real Korean text (not placeholders)

**lib/utils.ts** -- Utility functions:
- `cn(...inputs: ClassValue[]): string` using clsx + tailwind-merge (already installed)
- `cropStyle(crop?: CropData | null): React.CSSProperties` returning objectPosition, transform, transformOrigin per CLAUDE.md crop system
- `parseSiteConfig(rows: SiteConfig[]): Record<string, any>` to convert array of {id, value} rows into keyed object
  </action>
  <verify>
    <automated>cd /c/Users/frenc/Projects/kpeachgirl-app && npx tsc --noEmit lib/types.ts lib/constants.ts lib/i18n.ts lib/utils.ts 2>&1 | head -20</automated>
  </verify>
  <done>All 4 lib files exist, export correct types/values, compile without TypeScript errors. Satisfies FOUND-05, FOUND-06, FOUND-07.</done>
</task>

<task type="auto">
  <name>Task 2: Create shared UI components</name>
  <files>components/AgeGate.tsx, components/ModelCard.tsx, components/GroupCard.tsx, components/PhotoCropDisplay.tsx, components/Lightbox.tsx, components/Navbar.tsx, components/Footer.tsx, components/FilterBar.tsx, components/ProfilePills.tsx, components/CategoryStats.tsx</files>
  <action>
Create 10 shared components translating prototype inline styles to Tailwind + existing CSS classes from globals.css. Every component imports types from lib/types.ts. Convert prototype's inline styles to Tailwind classes using the design tokens already in tailwind.config.ts (bg-cream, text-charcoal, text-rose, border-sand, text-muted, etc.).

**components/AgeGate.tsx** ('use client') -- PUB-01:
- Props: `{ children: React.ReactNode }`
- Checks sessionStorage('age_verified') in useEffect. Returns null during SSR (prevents flash). If not verified, shows overlay. If verified, renders children.
- Overlay: fixed inset-0 z-[9999], card-bg panel with logo "Kpeachgirl" (serif, K + rose "peach" + girl), divider, "Age Verification Required" heading, disclaimer text, "I am 18 or older -- Enter" button (hover: bg-rose), "Leave Site" button, footer text "All models are 18+"
- Match prototype AgeGate component (line 349-367) exactly for layout/content
- Entrance animation: translate-y + opacity transition via state, 80ms delay

**components/ModelCard.tsx** ('use client') -- PUB-06, PUB-07:
- Props: `{ profile: Profile; cardSettings: CardSettings }`
- Wraps in `<Link href={/model/${profile.slug}}>` (not onClick)
- 135% padding-top aspect ratio container
- Profile image with crop via cropStyle() from utils. If vacation, apply grayscale filter.
- Bottom gradient overlay using cardSettings.overlayColor + overlayOpacity
- Top-left badges: Verified (sage, if showVerifiedBadge && profile.verified), Away (peach, if showAwayBadge && profile.vacation)
- Bottom: name (serif 24px white), subtitle from subtitleFields config (region, types[0], experience, attributes.age)
- Hover: card-img scale(1.06) transition, card-name letter-spacing -- these are already CSS in globals.css
- Fallback SVG placeholder if no profile_image (first letter of name)

**components/GroupCard.tsx** ('use client') -- PUB-08:
- Props: `{ group: Group; cardSettings: CardSettings }`
- Wraps in `<Link href={/group/${group.slug}}>` (not onClick)
- Same aspect ratio and overlay as ModelCard
- Top-left: DUO/TRIO/GROUP badge (rose bg, dark text) -- auto-calculate from member_ids.length unless group.badge_label set
- Bottom: group name + "{N} models" subtitle
- Hover behavior same as ModelCard

**components/PhotoCropDisplay.tsx** (server-safe, no 'use client'):
- Props: `{ src: string; alt: string; crop?: CropData | null; className?: string; fill?: boolean; width?: number; height?: number; priority?: boolean }`
- Renders next/image with cropStyle applied. Uses fill mode by default.
- If no src, renders placeholder div with muted background

**components/Lightbox.tsx** ('use client') -- supporting PUB-10:
- Props: `{ images: string[]; currentIndex: number; onClose: () => void; onNavigate: (index: number) => void }`
- Fixed overlay z-[2000], black 96% opacity, backdrop-blur
- Prev/next buttons (left/right arrows, border white 15% opacity), keyboard nav (Escape, ArrowLeft, ArrowRight)
- Current image (max 88vh/88vw, object-contain)
- Counter at bottom: "N / total"
- Click background to close (cursor-zoom-out)
- Body overflow hidden while open (useEffect cleanup)

**components/Navbar.tsx** (server component, no 'use client'):
- Sticky top nav matching prototype: logo "Kpeachgirl" (serif), links section with Browse and Admin button
- Mobile: hide nav-links, show mob-admin button
- Uses existing CSS classes: nav-pad, nav-links

**components/Footer.tsx** (server component):
- Logo + copyright line, matching prototype footer
- Uses footer-pad CSS class

**components/FilterBar.tsx** ('use client') -- supporting PUB-03, PUB-04, PUB-05:
- Props: `{ areas: string[]; search: string; onSearchChange: (s: string) => void; area: string; onAreaChange: (a: string) => void; verifiedOnly: boolean; onVerifiedChange: (v: boolean) => void; hideVacation: boolean; onHideVacationChange: (v: boolean) => void; modelCount: number }`
- Area chip buttons: "All" + area list. Active chip: bg-charcoal text-cream border-charcoal. Inactive: transparent text-muted border-sand. Uppercase, 11px, font-weight-700.
- Area filtering logic note: "All" shows all. "LA" or "OC" filter by parent_region. Specific areas filter by region.
- Verified checkbox + Available checkbox (right-aligned)
- Model count: "{N} model(s)" text below

**components/ProfilePills.tsx** (server-safe):
- Props: `{ pillGroups: PillGroup[]; data: Record<string, string[]> }` where data maps dataKey to values (e.g. { types: ['Fashion','Editorial'], compensation: ['Paid Only'] })
- Renders pill badges for each group. Border color from pill group config. If color is var(--charcoal), use var(--sand) border and var(--ink) text. Otherwise use color with 33 opacity border.

**components/CategoryStats.tsx** (server-safe):
- Props: `{ categories: CategorySection[]; attributes: Record<string, string | undefined>; experience?: string; region?: string }`
- Renders 3-column grid (profile-stats CSS class) with category sections
- Each section: rose uppercase title with border-bottom, then key-value rows
- Values come from attributes jsonb. Special cases: "exp" key maps to experience prop, "region" key maps to region prop. All others: `attributes[field.key] || '---'`
  </action>
  <verify>
    <automated>cd /c/Users/frenc/Projects/kpeachgirl-app && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>All 10 components exist, import from lib/types, compile without errors. AgeGate uses sessionStorage. ModelCard/GroupCard use Link for navigation. Lightbox has keyboard nav. FilterBar has search + area chips + toggles.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without TypeScript or build errors
- lib/types.ts exports all required interfaces
- lib/constants.ts exports all 6 default config objects
- lib/i18n.ts has 70+ translation keys in both EN and KO
- All 10 components exist in components/ directory
</verification>

<success_criteria>
- Every downstream plan (03-02 through 03-06) can import from lib/types, lib/constants, lib/utils without error
- AgeGate wraps content and checks sessionStorage
- ModelCard and GroupCard render with proper crop, badges, and Link navigation
- Lightbox has keyboard navigation (Escape, Arrow keys)
- FilterBar provides search, area chips, verified toggle, available toggle
- `npm run build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-public-pages/03-01-SUMMARY.md`
</output>
