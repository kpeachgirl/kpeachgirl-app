---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - tailwind.config.ts
  - postcss.config.mjs
  - next.config.ts
  - app/globals.css
  - app/layout.tsx
  - app/page.tsx
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04

must_haves:
  truths:
    - "npm run dev starts without errors and serves a page at localhost:3000"
    - "The dark luxury CSS variables (--cream, --warm, --sand, --charcoal, --ink, --muted, --rose, --rose-soft, --rose-mid, --peach, --sage, --card-bg, --input-bg) are present in :root in the browser"
    - "Cormorant Garamond and Manrope fonts load from next/font/google with no layout shift (CLS = 0)"
    - "Tailwind utility classes bg-cream, text-charcoal, text-rose, bg-card-bg, bg-input-bg, text-sage, font-serif, font-sans resolve correctly"
    - "globals.css contains grain overlay, card-img/model-card animations, slide-in/fade-up keyframes, and all responsive breakpoints"
    - "next.config.ts has remotePatterns for *.supabase.co storage"
    - "npm run build produces zero TypeScript errors and zero build failures"
  artifacts:
    - path: "package.json"
      provides: "Next.js 14.2.35, Tailwind v3, clsx, tailwind-merge, sharp dependencies"
      contains: "\"next\": \"14.2.35\""
    - path: "tsconfig.json"
      provides: "TypeScript strict mode configuration"
      contains: "\"strict\": true"
    - path: "tailwind.config.ts"
      provides: "Design token bridge from CSS variables to Tailwind utilities"
      exports: ["colors.cream", "colors.rose", "colors.sage", "colors.card-bg", "fontFamily.serif", "fontFamily.sans"]
    - path: "app/globals.css"
      provides: "CSS variables :root block, grain, animations, responsive breakpoints"
      contains: "--cream: #0e0d0c"
    - path: "app/layout.tsx"
      provides: "Root layout with Cormorant Garamond + Manrope font injection"
      contains: "cormorant.variable"
    - path: "next.config.ts"
      provides: "next/image remotePatterns for Supabase Storage"
      contains: "*.supabase.co"
  key_links:
    - from: "app/globals.css (:root)"
      to: "tailwind.config.ts (theme.extend.colors)"
      via: "var(--cream) references"
      pattern: "var\\(--cream\\)"
    - from: "app/layout.tsx (cormorant.variable)"
      to: "tailwind.config.ts (fontFamily.serif)"
      via: "var(--font-cormorant) CSS variable injected on <html>"
      pattern: "cormorant\\.variable"
---

<objective>
Scaffold the Next.js 14.2.35 project from scratch and configure the complete design system foundation: Tailwind v3 with all dark luxury design tokens, next/font/google with Cormorant Garamond + Manrope, and globals.css with CSS variables, grain texture, animations, and responsive breakpoints.

Purpose: Every component in every subsequent phase imports from this design system. Getting tokens, fonts, and layout classes right now prevents cascading visual bugs later.
Output: A runnable Next.js 14 app at localhost:3000 with the full dark luxury aesthetic applied, TypeScript strict mode active, and a next.config.ts remotePatterns placeholder ready for Supabase Storage in Phase 3.
</objective>

<execution_context>
@C:/Users/frenc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/frenc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/frenc/Projects/kpeachgirl-app/.planning/ROADMAP.md
@C:/Users/frenc/Projects/kpeachgirl-app/.planning/STATE.md
@C:/Users/frenc/Projects/kpeachgirl-app/CLAUDE.md

<!-- No prior SUMMARYs — this is Wave 1 of Phase 1, starting from scratch. -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 14.2.35 project and install dependencies</name>
  <files>
    package.json
    tsconfig.json
    next.config.ts
    postcss.config.mjs
    app/layout.tsx
    app/page.tsx
  </files>
  <action>
Run the following command from C:/Users/frenc/Projects/kpeachgirl-app's PARENT directory (C:/Users/frenc/Projects/), because create-next-app creates the project directory itself. If the kpeachgirl-app directory already contains files (like .planning/), run create-next-app into a temp directory and copy the generated files over instead — do NOT delete .planning/.

Option A (preferred — empty directory):
```
cd C:/Users/frenc/Projects
npx create-next-app@14.2.35 kpeachgirl-app \
  --typescript \
  --tailwind \
  --app \
  --no-src-dir \
  --import-alias "@/*" \
  --no-eslint \
  --yes
```

Option B (directory already has .planning/ files — scaffold into temp then merge):
```
cd C:/Users/frenc/Projects
npx create-next-app@14.2.35 kpeachgirl-app-temp \
  --typescript \
  --tailwind \
  --app \
  --no-src-dir \
  --import-alias "@/*" \
  --no-eslint \
  --yes

# Copy all scaffolded files EXCEPT node_modules into kpeachgirl-app
cp -r kpeachgirl-app-temp/. kpeachgirl-app/ --exclude=node_modules
cd kpeachgirl-app
npm install
rm -rf ../kpeachgirl-app-temp
```

After scaffolding, from C:/Users/frenc/Projects/kpeachgirl-app:

1. Check Tailwind version:
```
node -e "console.log(require('./node_modules/tailwindcss/package.json').version)"
```
If the version starts with "4.", downgrade immediately:
```
npm install tailwindcss@^3.4 postcss autoprefixer
```

2. Install additional dependencies:
```
npm install clsx tailwind-merge sharp
```

3. Replace next.config.ts with the following EXACT content (create-next-app may generate next.config.mjs — rename it to next.config.ts):

```typescript
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: '*.supabase.co',
        pathname: '/storage/v1/object/public/**',
      },
    ],
  },
}

export default nextConfig
```

4. Verify tsconfig.json has `"strict": true` in compilerOptions. create-next-app sets this by default. If missing, add it.

5. Replace app/page.tsx with a minimal smoke-test page (do NOT keep the default Next.js welcome page):

```typescript
export default function Home() {
  return (
    <main className="min-h-screen flex items-center justify-center">
      <p className="font-serif text-charcoal text-2xl">kpeachgirl</p>
    </main>
  )
}
```
  </action>
  <verify>
    <automated>cd C:/Users/frenc/Projects/kpeachgirl-app && node -e "const p=require('./package.json'); console.log('next:', p.dependencies.next, 'tw:', require('./node_modules/tailwindcss/package.json').version)"</automated>
  </verify>
  <done>package.json has next@14.2.x, Tailwind major version is 3.x, clsx and tailwind-merge are in dependencies, tsconfig.json has "strict": true, next.config.ts exports remotePatterns for *.supabase.co</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Configure Tailwind design tokens, globals.css, and root layout with fonts</name>
  <files>
    tailwind.config.ts
    app/globals.css
    app/layout.tsx
  </files>
  <behavior>
    - tailwind.config.ts must export colors for all 11 named tokens: cream, warm, sand, charcoal, ink, muted, rose, peach, sage, card-bg, input-bg — all referencing var(--token-name)
    - tailwind.config.ts must export fontFamily.serif referencing var(--font-cormorant) and fontFamily.sans referencing var(--font-manrope)
    - globals.css :root must define all 13 CSS variables (--cream through --input-bg including --rose-soft and --rose-mid)
    - globals.css must contain grain .grain::before rule with SVG feTurbulence data URI
    - globals.css must contain .card-img, .model-card:hover, .slide-in, .fade-up, .stagger-1 through .stagger-5, .gallery-item animations
    - globals.css must contain responsive breakpoints for .model-grid, .profile-hero, .profile-stats, .admin-slide at desktop/tablet/mobile
    - app/layout.tsx must import Cormorant_Garamond and Manrope from 'next/font/google', inject as CSS variables on &lt;html&gt;, apply grain class to &lt;body&gt;
  </behavior>
  <action>
Replace tailwind.config.ts with this EXACT content:

```typescript
import type { Config } from 'tailwindcss'

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
    './lib/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        cream:      'var(--cream)',
        warm:       'var(--warm)',
        sand:       'var(--sand)',
        charcoal:   'var(--charcoal)',
        ink:        'var(--ink)',
        muted:      'var(--muted)',
        rose:       'var(--rose)',
        peach:      'var(--peach)',
        sage:       'var(--sage)',
        'card-bg':  'var(--card-bg)',
        'input-bg': 'var(--input-bg)',
      },
      fontFamily: {
        serif: ['var(--font-cormorant)', 'Georgia', 'serif'],
        sans:  ['var(--font-manrope)', 'system-ui', 'sans-serif'],
      },
    },
  },
  plugins: [],
}

export default config
```

Replace app/globals.css with this EXACT content (replace everything create-next-app generated):

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

/* ─── Design Tokens ─────────────────────────────────────────── */
:root {
  --cream:       #0e0d0c;
  --warm:        #161514;
  --sand:        #2a2622;
  --charcoal:    #f0ebe5;
  --ink:         #e0d8ce;
  --muted:       #8a8078;
  --rose:        #d4758a;
  --rose-soft:   rgba(212, 117, 138, 0.12);
  --rose-mid:    rgba(212, 117, 138, 0.2);
  --peach:       #e0a08c;
  --sage:        #8fad8f;
  --card-bg:     #181716;
  --input-bg:    #1e1d1b;
}

* { margin: 0; padding: 0; box-sizing: border-box; }
body { background: var(--cream); color: var(--charcoal); }
html { color-scheme: dark; }

/* ─── Grain Texture ──────────────────────────────────────────── */
.grain::before {
  content: '';
  position: fixed;
  inset: 0;
  z-index: 9998;
  pointer-events: none;
  opacity: 0.025;
  background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E");
}

/* ─── Animations ─────────────────────────────────────────────── */
.card-img {
  transition: transform 1.2s cubic-bezier(0.16, 1, 0.3, 1), filter 0.6s ease;
}
.model-card:hover .card-img { transform: scale(1.06); }
.model-card:hover .card-name { letter-spacing: 0.04em; }

.slide-in { animation: slideIn 0.35s cubic-bezier(0.16, 1, 0.3, 1); }
@keyframes slideIn {
  from { transform: translateX(100%); opacity: 0; }
  to   { transform: translateX(0);    opacity: 1; }
}

.fade-up { animation: fadeUp 0.6s cubic-bezier(0.16, 1, 0.3, 1) both; }
@keyframes fadeUp {
  from { transform: translateY(24px); opacity: 0; }
  to   { transform: translateY(0);    opacity: 1; }
}

.stagger-1 { animation-delay: 0.05s; }
.stagger-2 { animation-delay: 0.1s; }
.stagger-3 { animation-delay: 0.15s; }
.stagger-4 { animation-delay: 0.2s; }
.stagger-5 { animation-delay: 0.25s; }

.gallery-item { break-inside: avoid; margin-bottom: 10px; cursor: pointer; overflow: hidden; }
.gallery-item img { transition: transform 0.8s cubic-bezier(0.16, 1, 0.3, 1); display: block; width: 100%; }
.gallery-item:hover img { transform: scale(1.03); }

input:focus, textarea:focus, select:focus { outline: none; border-color: var(--rose) !important; }
::selection { background: var(--rose-mid); }

/* ─── Layout Classes & Responsive Breakpoints ───────────────── */

/* Desktop (>1024px) */
.model-grid      { display: grid; grid-template-columns: repeat(4, 1fr); gap: 2px; }
.profile-hero    { display: grid; grid-template-columns: 1fr 1fr; min-height: 85vh; }
.profile-stats   { display: grid; grid-template-columns: repeat(3, 1fr); gap: 48px; }
.profile-gallery { columns: 3; column-gap: 10px; }
.admin-slide     { position: fixed; top: 0; right: 0; bottom: 0; width: 500px; }
.hero-title      { font-size: 72px; }
.hero-search     { width: 360px; }
.profile-name    { font-size: 64px; }
.admin-fields-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 10px 20px; }

/* Tablet (640px–1024px) */
@media (max-width: 1024px) {
  .model-grid      { grid-template-columns: repeat(2, 1fr); }
  .profile-hero    { grid-template-columns: 1fr; min-height: auto; }
  .profile-stats   { grid-template-columns: repeat(2, 1fr); gap: 32px; }
  .profile-gallery { columns: 2; }
  .admin-slide     { width: 420px; }
  .hero-title      { font-size: 56px; }
  .profile-name    { font-size: 48px; }
  .admin-fields-grid { grid-template-columns: 1fr 1fr; }
}

/* Mobile (<640px) */
@media (max-width: 640px) {
  .model-grid      { grid-template-columns: repeat(2, 1fr); gap: 1px; }
  .profile-hero    { grid-template-columns: 1fr; }
  .profile-stats   { grid-template-columns: 1fr; gap: 24px; }
  .profile-gallery { columns: 2; column-gap: 6px; }
  .admin-slide     { width: 100%; left: 0; }
  .admin-save-bar  { width: 100%; left: 0; }
  .admin-fields-grid { grid-template-columns: 1fr; }
  .hero-title      { font-size: 36px; }
  .profile-name    { font-size: 36px; }
  .filter-scroll   { flex-wrap: nowrap; overflow-x: auto; -webkit-overflow-scrolling: touch; }
}
```

Replace app/layout.tsx with this EXACT content:

```typescript
import type { Metadata } from 'next'
import { Cormorant_Garamond, Manrope } from 'next/font/google'
import './globals.css'

const cormorant = Cormorant_Garamond({
  subsets: ['latin'],
  weight: ['300', '400', '500', '600', '700'],
  style: ['normal', 'italic'],
  variable: '--font-cormorant',
  display: 'swap',
})

const manrope = Manrope({
  subsets: ['latin'],
  weight: ['300', '400', '500', '600', '700', '800'],
  variable: '--font-manrope',
  display: 'swap',
})

export const metadata: Metadata = {
  title: 'Kpeachgirl — Editorial Model Directory',
  description: 'LA/OC editorial model directory',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className={`${cormorant.variable} ${manrope.variable}`}>
      <body className="bg-cream text-charcoal font-sans grain">
        {children}
      </body>
    </html>
  )
}
```
  </action>
  <verify>
    <automated>cd C:/Users/frenc/Projects/kpeachgirl-app && npx tsc --noEmit && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>npx tsc --noEmit exits 0 with no errors. npm run build completes successfully. The browser at localhost:3000 renders "kpeachgirl" in a serif font on a near-black background (#0e0d0c). Browser DevTools computed styles show --cream, --rose, --sage are defined on :root.</done>
</task>

</tasks>

<verification>
After both tasks complete, run the full phase verification:

```bash
cd C:/Users/frenc/Projects/kpeachgirl-app

# 1. TypeScript strict mode is on
node -e "const t=require('./tsconfig.json'); console.log('strict:', t.compilerOptions.strict)"
# Expected: strict: true

# 2. Next.js and Tailwind versions are correct
node -e "const p=require('./package.json'); console.log('next:', p.dependencies.next)"
node -e "console.log('tailwind:', require('./node_modules/tailwindcss/package.json').version)"
# Expected: next: 14.2.35, tailwind: 3.x.x

# 3. All 11 Tailwind color tokens present in config
node -e "const c=require('./tailwind.config.ts'); console.log(Object.keys(c.default.theme.extend.colors))"
# Expected: [ 'cream', 'warm', 'sand', 'charcoal', 'ink', 'muted', 'rose', 'peach', 'sage', 'card-bg', 'input-bg' ]

# 4. globals.css has all 13 CSS variables
grep -c '\-\-' app/globals.css
# Expected: 13 or more

# 5. Build passes
npm run build
# Expected: zero errors, "Route (app) /" listed

# 6. remotePatterns in next.config.ts
grep "supabase.co" next.config.ts
# Expected: hostname: '*.supabase.co'
```
</verification>

<success_criteria>
1. `npm run dev` starts without errors and serves a page at localhost:3000
2. The page renders text in Cormorant Garamond (serif) on a #0e0d0c near-black background
3. Browser DevTools Elements panel shows all CSS variables defined on :root (--cream, --rose, --sage, --card-bg, --input-bg visible)
4. `npm run build` exits with zero errors and zero TypeScript warnings
5. `tailwind.config.ts` exports 11 color tokens and 2 font families, all referencing CSS variables
6. `globals.css` contains .grain::before, @keyframes slideIn, @keyframes fadeUp, and responsive @media blocks
7. `next.config.ts` has remotePatterns with *.supabase.co wildcard
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` with:
- Files created/modified with final state description
- Tailwind version confirmed (3.x)
- Any deviations from this plan (e.g., if create-next-app generated slightly different file names)
- Build output (pass/fail + any warnings)
- Decisions made (e.g., handling of pre-existing .planning/ directory)
</output>
